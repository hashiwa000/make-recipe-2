{
  "name": "ai-weekly-menu-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run",
    "copy:openapi": "mkdir -p openapi && cp -f ../specs/001-ai-weekly-menu/contracts/openapi.yaml ./openapi/openapi.yaml"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "fastify": "^4.26.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.6.3",
    "vitest": "^1.6.0"
  }
}
